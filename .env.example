# ========================================
# Docker Compose 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# ========================================

# ==================== 数据库配置 ====================
# MySQL Root 密码 (必填)
MYSQL_ROOT_PASSWORD=your_secure_root_password_here

# 应用数据库名称
MYSQL_DATABASE=seed

# 应用数据库用户 (非root)
MYSQL_USER=seed_app

# 应用数据库密码 (必填)
MYSQL_PASSWORD=your_secure_app_password_here

# ==================== Redis 配置 ====================
# Redis 密码 (可选，生产环境建议设置)
REDIS_PASSWORD=

# ==================== JWT 配置 ====================
# JWT 密钥 (必填，至少32字符，建议使用Base64编码)
# 生成命令: openssl rand -base64 64
JWT_SECRET=your_base64_encoded_secret_key_at_least_32_bytes

# ==================== JVM 配置 ====================
# JVM 参数
JAVA_OPTS=-Xms512m -Xmx1024m -XX:+UseG1GC

# ==================== 其他配置 ====================
# 服务端口
SERVER_PORT=8080

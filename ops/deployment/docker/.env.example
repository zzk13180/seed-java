# ========================================
# Seed Cloud Docker 环境变量模板
# ========================================
# 使用方法: 复制本文件为 .env，按需修改
#   cp .env.example .env
#
# 配置分类说明:
#   [必需] - 启动服务前必须配置
#   [推荐] - 生产环境强烈建议修改
#   [可选] - 有默认值，按需覆盖
# ========================================

# ---------- 通用 [可选] ----------
# Spring Boot 激活的 profile（docker / prod / test）
SPRING_PROFILES_ACTIVE=docker
# 镜像标签
IMAGE_TAG=latest
# 构建时是否跳过测试
SKIP_TESTS=true

# ---------- Nacos ----------
NACOS_PORT=8848
# Nacos 是否启用配置中心（true 则从 Nacos 拉取共享配置）
NACOS_CONFIG_ENABLED=false
# Nacos 认证（生产环境必须启用，以下为示例值）
# 警告：请在生产环境使用强密钥！
NACOS_AUTH_ENABLE=true
NACOS_AUTH_TOKEN=SecretKey012345678901234567890123456789012345678901234567890123456789
NACOS_AUTH_IDENTITY_KEY=serverIdentity
NACOS_AUTH_IDENTITY_VALUE=security
# Nacos JVM 参数
NACOS_JVM_XMS=256m
NACOS_JVM_XMX=512m

# ---------- Redis ----------
REDIS_PORT=6379
# 留空则不设置密码
REDIS_PASSWORD=

# ---------- PostgreSQL ----------
POSTGRES_PORT=5432
POSTGRES_DATABASE=seed
POSTGRES_USERNAME=postgres
# ⚠️ 生产环境必须修改为强密码
POSTGRES_PASSWORD=postgres

# ---------- MinIO ----------
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minioadmin
# ⚠️ 生产环境必须修改为强密码
MINIO_ROOT_PASSWORD=minioadmin123

# ---------- Sentinel ----------
SENTINEL_PORT=8858

# ---------- 应用端口 ----------
GATEWAY_PORT=8080
AUTH_PORT=9100
SYSTEM_PORT=9200
FRONTEND_PORT=80
